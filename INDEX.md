# 📦 HardwareInfoProvider - Windows & Linux Edition

## Спрощена версія тільки для Windows і Linux

### 🎯 Що це?

Легка і проста бібліотека Qt для отримання інформації про апаратне забезпечення.

**Підтримка:** Тільки Windows і Linux (прибрано Android, macOS, iOS)

---

## 📥 Файли для завантаження

### 🔥 Основні файли (потрібні завжди)
1. [**HardwareInfoProvider.h**](HardwareInfoProvider.h) - заголовочний файл (60 рядків)
2. [**HardwareInfoProvider.cpp**](HardwareInfoProvider.cpp) - реалізація (620 рядків)
3. [**main.cpp**](main.cpp) - приклад використання

### ⚙️ Конфігурація збірки
4. [**CMakeLists.txt**](CMakeLists.txt) - для CMake
5. [**hwinfo.pro**](hwinfo.pro) - для qmake

### 📚 Документація
6. [**README.md**](README.md) - повна документація
7. [**QUICKSTART.md**](QUICKSTART.md) - швидкий старт

---

## ✨ Можливості

### ✅ Windows
- CPU: назва, ядра, частота (з реєстру)
- RAM: загальна, доступна, використана
- GPU: назва (DirectX DXGI)
- VRAM: загальна, використана, вільна (DXGI 1.4, Win 10+)
- Диски: список, тип (SSD/HDD через WMI), розмір, вільне місце

### ✅ Linux
- CPU: назва, ядра, частота (з /proc/cpuinfo)
- RAM: загальна, доступна, використана (sysinfo)
- GPU: назва (lspci)
- VRAM: загальна і використана (nvidia-smi або sysfs)
- Диски: список, тип (SSD/HDD через /sys/block), розмір, вільне місце

---

## 🚀 Швидкий старт

### Windows
```bash
# Відкрийте hwinfo.pro у Qt Creator → Run
```

### Linux
```bash
sudo apt install qt6-base-dev pciutils
qmake6 hwinfo.pro && make && ./hwinfo
```

---

## 💻 Приклад коду

```cpp
#include <QCoreApplication>
#include <QDebug>
#include "HardwareInfoProvider.h"

int main(int argc, char *argv[])
{
    QCoreApplication app(argc, argv);
    
    HardwareInfoProvider hw;
    
    // Вся інформація
    qDebug().noquote() << hw.getAllSystemInfo();
    
    // Окремі параметри
    qDebug() << "Platform:" << hw.getPlatformName();
    qDebug() << "CPU:" << hw.getCPUName();
    qDebug() << "Cores:" << hw.getCPUCores();
    qDebug() << "RAM:" << hw.formatBytes(hw.getTotalRAM());
    qDebug() << "GPU:" << hw.getGPUName();
    qDebug() << "VRAM:" << hw.getGPUMemoryMB() << "MB";
    qDebug() << "VRAM Used:" << hw.getGPUUsedMemoryMB() << "MB";
    
    // Диски
    qDebug() << "Total Disk:" << hw.formatBytes(hw.getTotalDiskSpace());
    qDebug() << "Free Disk:" << hw.formatBytes(hw.getFreeDiskSpace());
    
    return 0;
}
```

---

## 📊 Повний список методів

### ОС
- `QString getPlatformName()` → "Windows" або "Linux"
- `QString getOSInfo()` → назва ОС
- `QString getKernelVersion()` → версія ядра
- `QString getArchitecture()` → архітектура (x86_64, ...)

### CPU
- `QString getCPUName()` → назва процесора
- `int getCPUCores()` → кількість ядер
- `int getCPUFrequencyMHz()` → частота в MHz
- `double getCPUFrequencyGHz()` → частота в GHz

### RAM
- `quint64 getTotalRAM()` → загальна (bytes)
- `quint64 getAvailableRAM()` → доступна (bytes)
- `quint64 getUsedRAM()` → використана (bytes)
- `double getRAMUsagePercent()` → відсоток використання

### GPU
- `QString getGPUInfo()` → повна інформація
- `QString getGPUName()` → назва GPU
- `quint64 getGPUMemoryMB()` → загальна VRAM (MB)
- `quint64 getGPUUsedMemoryMB()` → використана VRAM (MB)
- `double getGPUMemoryUsagePercent()` → відсоток використання VRAM

### Утиліти
- `QString formatBytes(quint64)` → форматування байтів
- `QString getAllSystemInfo()` → вся інформація

---

## 📈 Приклад виводу

```
=== Системна інформація ===

Платформа: Windows

Операційна система:
  Назва: Windows 11 Pro
  Ядро: 10.0.22621
  Архітектура: x86_64

Процесор:
  Модель: Intel(R) Core(TM) i7-10700K CPU @ 3.80GHz
  Ядра: 16
  Частота: 3800 MHz (3.80 GHz)

Оперативна пам'ять:
  Загальна: 32.00 GB
  Доступна: 18.45 GB
  Використано: 13.55 GB
  Використання: 42.3%

Відеокарта:
  Назва: NVIDIA GeForce RTX 3080
  VRAM Загальна: 10240 MB (10.00 GB)
  VRAM Використано: 3584 MB (3.50 GB)
  VRAM Вільно: 6656 MB (6.50 GB)
  VRAM Використання: 35.0%

Диски:
  C:\ (NTFS)
    Тип: SSD
    Розмір: 500.00 GB
    Вільно: 125.50 GB (25.1%)
    Використано: 374.50 GB (74.9%)
  D:\ (NTFS)
    Тип: HDD
    Розмір: 2.00 TB
    Вільно: 1.20 TB (60.0%)
    Використано: 819.20 GB (40.0%)

  Всього на дисках:
    Загальний розмір: 2.49 TB
    Вільно: 1.31 TB
    Використано: 1.18 TB (47.5%)

===========================
```

---

## 🔧 Вимоги

### Windows
- Qt 6.2+
- MSVC 2019+ або MinGW
- Windows SDK

### Linux
- Qt 6.2+
- GCC 9+ або Clang 10+
- `pciutils`: `sudo apt install pciutils`
- (Опціонально) `nvidia-utils` для NVIDIA VRAM

---

## 📦 Додавання у проект

### Варіант 1: Прямо у проект
```
YourProject/
├── main.cpp
├── HardwareInfoProvider.h
├── HardwareInfoProvider.cpp
└── YourProject.pro
```

### Варіант 2: Як підмодуль
```
YourProject/
├── main.cpp
├── hardware/
│   ├── HardwareInfoProvider.h
│   └── HardwareInfoProvider.cpp
└── YourProject.pro
```

---

## 🎯 Переваги спрощеної версії

✅ Менше коду (620 рядків замість 775)  
✅ Простіша підтримка (2 платформи замість 5)  
✅ Швидша компіляція  
✅ Легше розуміти код  
✅ Без Qt private headers  
✅ Без JNI/Android залежностей  

---

## 📊 Статистика

| Параметр | Значення |
|----------|----------|
| Заголовок | 60 рядків |
| Реалізація | 620 рядків |
| Всього коду | 680 рядків |
| Платформи | 2 (Windows, Linux) |
| Методів API | 21 |
| Залежності | Тільки Qt Core |

---

## 🔄 Що змінилося від повної версії?

### ❌ Прибрано
- Android підтримка
- macOS підтримка  
- iOS підтримка
- Інформація про батарею
- Інформація про екран/DPI
- Qt private headers
- JNI код
- QGuiApplication залежність

### ✅ Залишилося
- Windows підтримка (повна)
- Linux підтримка (повна)
- CPU інформація
- RAM інформація
- GPU інформація
- VRAM інформація (включно з використаною!)

---

## 🐛 Troubleshooting

### Windows
**Проблема:** VRAM Used = 0  
**Рішення:** Потрібен Windows 10+

**Проблема:** Помилка компіляції з dxgi.lib  
**Рішення:** Встановіть Windows SDK

### Linux
**Проблема:** GPU information not available  
**Рішення:** `sudo apt install pciutils`

**Проблема:** VRAM Used = 0 (NVIDIA)  
**Рішення:** `sudo apt install nvidia-utils`

**Проблема:** VRAM Used = 0 (AMD)  
**Рішення:** Перевірте `/sys/class/drm/card*/device/mem_info_vram_used`

---

## 💡 Use Cases

### 1. Системний монітор
```cpp
QTimer *timer = new QTimer();
connect(timer, &QTimer::timeout, [&]() {
    HardwareInfoProvider hw;
    qDebug() << "RAM:" << hw.getRAMUsagePercent() << "%";
    qDebug() << "VRAM:" << hw.getGPUMemoryUsagePercent() << "%";
});
timer->start(1000);
```

### 2. Діагностика системи
```cpp
HardwareInfoProvider hw;
if (hw.getRAMUsagePercent() > 90) {
    qWarning() << "Low memory!";
}
```

### 3. Логування конфігурації
```cpp
HardwareInfoProvider hw;
qInfo() << "App started on:" << hw.getPlatformName();
qInfo() << "CPU:" << hw.getCPUName();
qInfo() << "RAM:" << hw.formatBytes(hw.getTotalRAM());
```

---

## 📚 Додаткова документація

Дивіться [README.md](README.md) для детальної документації всіх методів.

---

## 📜 Ліцензія

Вільне використання. Без обмежень.

---

## ⭐ Чому саме ця версія?

1. **Проста** - тільки 2 платформи
2. **Чиста** - без зайвих залежностей
3. **Швидка** - компілюється за секунди
4. **Повна** - всі важливі функції є
5. **Готова** - працює out of the box

---

**Версія:** 2.0 Simplified  
**Дата:** Листопад 2025  
**Підтримка:** Windows + Linux  
**Статус:** ✅ Production Ready

**Насолоджуйтесь! 🎉**
